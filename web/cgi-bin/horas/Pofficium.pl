#!/usr/bin/perl

# Use the FindBin module to get the absolute path of the directory
# containing this script, and load the officium.pl module located
# in that directory.
use FindBin qw($Bin);
require "$Bin/officium.pl";
