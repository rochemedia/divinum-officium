//*** Javascript functions

// The 'startup' function is called from htmlhead and initializes various form elements
// based on the current date and user input. It sets the browser time, checks if date1 is set,
// scrolls to specific elements, and submits the form if necessary.
function startup() {
  //...
}

// The 'pset' function creates a hidden input element, sets its attributes, and appends it to the form.
// It then sets the command value to "setup" followed by the input parameter 'p' and submits the form.
function pset(p) {
  //...
}

// The 'hset' function clears radio buttons, sets the callers value, command value, action, and target,
// and submits the form. If the second parameter 'd' is provided, it also sets the date value.
function hset(p, d) {
  //...
}

// The 'appendix' function sets the command value to "Appendix " followed by the input parameter 'a' and submits the form.
function appendix(a) {
  //...
}

// The 'defunctorum' function sets various form values and submits the form to navigate to a specific hour of the Office for the Dead.
function defunctorum(hour) {
  //...
}

// The 'callcompare' function sets the action to "Cofficium.pl" and submits the form.
function callcompare() {
  //...
}

// The 'clearradio' function sets the value of the popup radio button to 0 and resets the action and target of the form.
function clearradio() {
  //...
}

// The 'linkit' function sets various form values based on the input parameters, and submits the form to either open a new tab or navigate to a new page.
function linkit(name, ind, lang) {
  //...
}

// The 'okbutton' function resets the action, target, and command of the form and submits it.
function okbutton() {
  //...
}

// The 'parchange' function checks the command value and resets the radio button and submits the form if necessary.
function parchange() {
  //...
}

// The 'callkalendar' function sets the action to "kalendar.pl" and submits the form.
function callkalendar() {
  //...
}

// The 'callbrevi' function sets the date value and submits the form to navigate to the breviary page.
function callbrevi(date) {
  //...
}

// The 'callmissa' function sets the action to "../missa/missa.pl" and submits the form.
function callmissa() {
  //...
}

// The 'prevnext' function calculates the new date based on the input parameter 'ch' and sets the date value of the form.
function prevnext(ch) {
  //...
}
